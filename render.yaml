services:
  - type: web
    name: ariba-report-generator
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run ariba_daily_report_streamlit.py --server.port=$PORT --server.address=0.0.0.0